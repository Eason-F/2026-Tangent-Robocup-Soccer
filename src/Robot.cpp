#include <Robot.hpp>

void Robot::run() {
    // odometry.updatePosition();
    // Serial.print("X: "); Serial.print(odometry.getX()); Serial.print("    "); Serial.print("Y: "); Serial.println(odometry.getY());

    // irSensor.updateReadings();
    // Serial.print("Direction: "); Serial.print(degrees(irSensor.signalVec.angle)); Serial.print("    "); 
    // Serial.print("Strength: "); Serial.println(irSensor.signalVec.length);
}